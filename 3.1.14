#include <stdio.h>

    int page_table[8] = {3, 5, 1, 7, 2, 6, 0, 4};  // Page → Frame mapping
    int logical_address;

    printf("Logical Address Space = %d bytes\n", logical_size);
    printf("Page Size = %d bytes\n", page_size);
    printf("Number of Pages = %d\n", num_pages);
    printf("Number of Frames = %d\n", num_frames);

    printf("Page Table:\n");
    for (int i = 0; i < num_pages; i++) {
        printf("Page %d -> Frame %d\n", i, page_table[i]);
    }

    printf("Enter a logical address (0 - %d): ", logical_size - 1);
    scanf("%d", &logical_address);

    if (logical_address < 0 || logical_address >= logical_size) {
        printf("Invalid logical address\n");
        return 0;
    }

    int page_number = logical_address / page_size;
    int offset = logical_address % page_size;
    int frame_number = page_table[page_number];
    int physical_address = frame_number * page_size + offset;

    printf("Logical Address %d is in:\n", logical_address);
    printf("Page Number = %d\n", page_number);
    printf("Offset = %d\n", offset);
    printf("Frame Number from Page Table = %d\n", frame_number);
    printf("Physical Address = (Frame %d * %d) + %d = %d\n",
           frame_number, page_size, offset, physical_address);

    return 0;
}

